apiVersion: v1
kind: ConfigMap
metadata:
  name: zobsai-frontend-config
  namespace: dev-zobsai
data:
  # Add dev-specific frontend config values here
  NEXT_PUBLIC_API_URL: "https://api.dev.zobsai.com"
