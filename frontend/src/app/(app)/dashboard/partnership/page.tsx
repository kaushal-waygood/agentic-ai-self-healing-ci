import React from 'react';
import Partnership from './components/partnership';

export const metadata = {
  title: 'Partnership | Zobsai',
};

const page = () => {
  return <Partnership />;
};

export default page;
